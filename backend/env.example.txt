PORT=5000
NODE_ENV=development
# MongoDB Connection String (you can use either format)
# Option 1: With database name (recommended - creates/uses 'techblooms' database)
MONGODB_URI=mongodb://localhost:27017/techblooms
# Option 2: Without database name (uses default database, database will be created when first document is saved)
# MONGODB_URI=mongodb://localhost:27017/
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=7d

